GEOSPATIAL E-PORTFOLIO — FIXED VERSION

Open index.html in a browser.

IMPORTANT:
- Keep index.html, style.css and script.js in the same folder.
- Keep the assets folder beside them.
- Your profile photo is already included at assets/images/preeti-profile.jpeg.
- Do not open only index.html from inside the ZIP preview. Extract the ZIP first.

The original problem shown in the screenshot was that the stylesheet was not being loaded, so the browser displayed the raw HTML/default browser styling. This version uses root-relative local CSS/JS paths to avoid that folder/path mismatch.
